<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required by the `camera` plugin (kyc-liveness's in-app live preview,
         BUG-02) — direct in-process camera access, unlike image_picker's
         "hand off to the OS camera app" flow which didn't need this held by
         our own app. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Required for every API call. Flutter's template declares INTERNET only
         in the debug and profile manifests (for the dev tooling), so a RELEASE
         build without this line is denied every socket by the OS: debug works,
         the shipped APK/AAB cannot reach the backend at all, and no HTTP client
         can report it as anything more useful than a connection error. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:label="Kudimata Invest"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required by url_launcher (legal_screen.dart's document
             download-url hand-off) so ACTION_VIEW can resolve an https
             handler on Android 11+ package-visibility restrictions:
             https://pub.dev/packages/url_launcher#configuration -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <!-- Required by url_launcher (help_support_screen.dart's email/call
             support rows) so ACTION_VIEW can resolve mailto/tel handlers on
             Android 11+ package-visibility restrictions. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="mailto"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tel"/>
        </intent>
    </queries>
</manifest>

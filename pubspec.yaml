name: kudimata_invest
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+43

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  fl_chart: ^1.2.0
  flutter_svg: ^2.3.0
  go_router: ^17.3.0

  # Live in-app camera preview + capture for kyc-liveness's selfie step.
  # Replaced image_picker (2026-08-14, BUG-02): that package's
  # ImageSource.camera hands off to the OS's separate camera app/activity —
  # no live preview inside the app's own circular frame, and capture
  # happened on a different screen entirely. camera gives a real
  # CameraController + CameraPreview widget rendered in place.
  camera: ^0.11.2

  # Device file picker for kyc-id's ID upload (JPG/PNG/PDF from device
  # storage — image_picker above only covers camera/gallery images, not
  # PDF, so this screen needs its own picker rather than reusing that one).
  file_picker: ^8.1.2

  # Shared API client infra (lib/data/api/) — HTTP client w/ interceptor
  # support, and secure on-device token storage. No state-management package
  # added alongside these: screens consume repositories via FutureBuilder,
  # see lib/data/api/README.md.
  dio: ^5.7.0
  flutter_secure_storage: ^9.2.2

  # SHA-256 for PasscodeStore's salted passcode hash (lib/data/api/passcode_store.dart)
  # — already resolved transitively (via dio/flutter_secure_storage); pinned
  # explicitly here since we import it directly.
  crypto: ^3.0.6

  # Legal screen: opens a presigned document-download URL externally — there
  # is no in-app PDF viewer, so launching the browser/OS handler is the
  # minimal appropriate tool for that one seam.
  url_launcher: ^6.3.2

  # Plain (non-interceptor) HTTP client for kyc-id's direct-to-S3 presigned
  # PUT upload — deliberately NOT the shared ApiClient's Dio instance, whose
  # auth interceptor would attach this app's own Authorization header to the
  # S3 request and break the presigned-URL signature.
  http: ^1.2.2

  # Refer & earn's "Share invite" (lib/screens/account/refer_earn_screen.dart)
  # — opens the real OS share sheet for the referral code/message, alongside
  # the existing trackShare() attribution ping.
  share_plus: ^12.0.2

  # Small local key-value persistence for the last ~5 recent search terms
  # (lib/screens/home/search_screen.dart) — purely on-device history, not a
  # backend concern.
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

# Launcher icon — the Kudimata mark (white) on brand purple; adaptive on Android.
flutter_launcher_icons:
  android: "ic_launcher"
  ios: true
  remove_alpha_ios: true
  image_path: "assets/icon/icon.png"
  adaptive_icon_background: "#670099"
  adaptive_icon_foreground: "assets/icon/icon_foreground.png"
  web:
    generate: true
    image_path: "assets/icon/icon.png"
    background_color: "#670099"
    theme_color: "#670099"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/brand/

  # Bundled Space Grotesk (sliced from the official variable font) so the app
  # never depends on a runtime font download — renders correctly offline / release.
  fonts:
    - family: Space Grotesk
      fonts:
        - asset: assets/fonts/SpaceGrotesk-Regular.ttf
          weight: 400
        - asset: assets/fonts/SpaceGrotesk-Medium.ttf
          weight: 500
        - asset: assets/fonts/SpaceGrotesk-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/SpaceGrotesk-Bold.ttf
          weight: 700

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
